import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  scrollContainer: {
    marginBottom: 16,
  },
  scrollContent: {
    paddingHorizontal: 16,
    paddingTop: 16,
  },
  buttonFixed: {
    marginBottom: 20,
    marginHorizontal: 20,
  },
});
